# ── 0G mainnet (client-visible) ──────────────────────────────
NEXT_PUBLIC_ZG_CHAIN_ID=16661
NEXT_PUBLIC_ZG_RPC_URL=https://evmrpc.0g.ai
NEXT_PUBLIC_ZG_EXPLORER=https://chainscan.0g.ai
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=

# Deployed contract addresses (fill after Phase 2 deploy)
NEXT_PUBLIC_HECKLE_CHARACTERS=
NEXT_PUBLIC_HECKLE_EVENTS=
NEXT_PUBLIC_HECKLE_TAKES=

# ── Server-side (never exposed to client) ────────────────────
ZG_RPC_URL=https://evmrpc.0g.ai
ZG_STORAGE_INDEXER=https://indexer-storage-turbo.0g.ai

# Deployer key for `forge create` (Phase 2) — DO NOT COMMIT
DEPLOYER_PRIVATE_KEY=

# Inference-agent signer: pays 0G Storage fees + commits Take roots on-chain
AGENT_PRIVATE_KEY=

# 0G Compute provider override (optional; agent discovers via listService otherwise)
ZG_COMPUTE_PROVIDER=
ZG_COMPUTE_MODEL=
